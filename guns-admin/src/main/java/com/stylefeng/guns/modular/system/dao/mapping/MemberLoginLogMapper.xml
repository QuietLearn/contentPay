<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.stylefeng.guns.modular.system.dao.MemberLoginLogMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.stylefeng.guns.modular.system.model.MemberLoginLog">
        <id column="id" property="id" />
        <result column="logname" property="logname" />
        <result column="appId" property="appId" />
        <result column="appVer" property="appVer" />
        <result column="channel" property="channel" />
        <result column="memberid" property="memberid" />
        <result column="createtime" property="createtime" />
        <result column="succeed" property="succeed" />
        <result column="message" property="message" />
        <result column="ip" property="ip" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, logname, appId, appVer, channel, memberid, createtime, succeed, message, ip
    </sql>

    <select id="getLoginLogs" resultType="map" parameterType="com.baomidou.mybatisplus.plugins.Page">
        select * from cps_member_login_log where 1 = 1
        <if test="beginTime != null and beginTime !='' and endTime != null and endTime != ''">
            and (createTime between CONCAT(#{beginTime},' 00:00:00') and CONCAT(#{endTime},' 23:59:59'))
        </if>
        <if test="logName != null and logName !=''">
            and logname like CONCAT('%',#{logName},'%')
        </if>
        <choose>
            <when test="orderByField != null and orderByField !=''">
                <choose>
                    <when test="isAsc == true">
                        order by ${orderByField} ASC
                    </when>
                    <otherwise>
                        order by ${orderByField} DESC
                    </otherwise>
                </choose>
            </when>
            <otherwise>
                order by createtime DESC
            </otherwise>
        </choose>
    </select>


    <select id="selectMaxCreatime" resultType="date" parameterType="int">
        select max(createtime)
        from cps_member_login_log
        where memberId = #{memberId}
    </select>

    <select id="selectLoginLogByCreateTime" resultMap="BaseResultMap" parameterType="map">
        select <include refid="Base_Column_List"/>
        from cps_member_login_log
        where memberId = #{memberId}
        and createtime = #{creatime}
    </select>
</mapper>
